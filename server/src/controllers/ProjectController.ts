import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient()

module.exports = {

    CreateProject: async( req, res ) => {
        const {  } = req.body

        try{

        } catch(err){


        } finally{


        }
    }
}